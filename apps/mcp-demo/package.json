{
  "name": "@split402/mcp-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "bundle": "tsx src/bundle.ts",
    "discover-external-x402": "tsx src/discover-external-x402.ts",
    "prepare-external-x402-offer": "tsx src/prepare-external-x402-offer.ts",
    "prepare-external-x402-receipt": "tsx src/prepare-external-x402-receipt.ts",
    "validate-external-x402-artifacts": "tsx src/validate-external-x402-artifacts.ts",
    "gateway": "tsx src/gateway.ts",
    "gateway:smoke": "tsx src/gateway-smoke.ts",
    "lint": "eslint .",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@split402/agent-sdk": "workspace:*",
    "@split402/protocol": "workspace:*",
    "@split402/router": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  }
}
